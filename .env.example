# Copy to .env and fill in via scripts/provision-acm-hotels-deploy.sh (issue #3).
# Secrets (DEPLOY_SSH_KEY, GHCR_TOKEN, SANITY_REVALIDATE_SECRET) are never
# stored here — they live only in GitHub Actions secrets / the container's
# runtime env.

NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=

# Shared secret configured on the Sanity webhook (project settings > API >
# Webhooks) that POSTs to /api/revalidate on publish — see issue #6.
SANITY_REVALIDATE_SECRET=

# Read-only (Viewer) API token the server-side Sanity client authenticates
# with. Required: the dataset's anonymous-read grant can't see documents
# whose _id contains a "." (e.g. "cooperationModel.management") — see
# docs/infrastructure.md.
SANITY_API_TOKEN=

# Separate, narrower-scoped API token (Editor role, Create permission only)
# used solely by POST /api/leads to write lead-form submissions as `lead`
# documents. Deliberately not the same token as SANITY_API_TOKEN above, so a
# leaked read token can never be used to write data. Without this set, the
# lead form's submissions fail with a clear 500 instead of a silent no-op.
SANITY_LEADS_WRITE_TOKEN=

# Deploy target, recorded for the wizard's re-run convenience.
SSH_HOST=
SSH_USER=
DOMAIN=
